//
//  AntigravityRoutingService.swift
//  Quotio
//
//  Obsolete. Antigravity support is now switch + quota only (no routing).
//

import Foundation

// Unused, kept only to avoid breaking Xcode project file references.
actor AntigravityRoutingService {
    static let shared = AntigravityRoutingService()
    private init() {}
}
